#Required gems to be installed

gem install em-http-request
gem install simple_oauth

Enter the following in stream_consumer.rb

consumer_key    
consumer_secret
access_token
access_token_secret

Run stream_consumer.rb
